"""Local control owner and attach client.

Import from the owning submodule (``contract``, ``server``, ``client``,
``daemon``). This package init stays import-light so ``contract`` remains
a leaf.
"""

from __future__ import annotations

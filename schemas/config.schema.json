{
  "$defs": {
    "CatalogPrefs": {
      "description": "``[catalog]``: host session list (every shipped adapter, minus ignore).",
      "properties": {
        "ignore": {
          "description": "Adapter ids omitted from the host catalog.",
          "items": {
            "type": "string"
          },
          "title": "Ignore",
          "type": "array"
        },
        "roots": {
          "additionalProperties": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "description": "Store path overrides keyed by adapter id.",
          "title": "Roots",
          "type": "object"
        }
      },
      "title": "CatalogPrefs",
      "type": "object"
    },
    "ExportPrefs": {
      "description": "``[export]``: default session-export profile id.",
      "properties": {
        "default_profile": {
          "default": "",
          "description": "Export profile id. Empty uses archive-full.",
          "title": "Default Profile",
          "type": "string"
        }
      },
      "title": "ExportPrefs",
      "type": "object"
    },
    "HudPrefs": {
      "description": "``[hud]``: desktop palette only.",
      "properties": {
        "window_mode": {
          "default": false,
          "description": "Start the HUD as a normal window.",
          "title": "Window Mode",
          "type": "boolean"
        },
        "global_shortcut": {
          "default": "",
          "description": "Summon chord (Cmd+Shift+A / Ctrl+Shift+A when empty).",
          "title": "Global Shortcut",
          "type": "string"
        },
        "desktop_notifications": {
          "default": true,
          "description": "Desktop notifications for session status changes.",
          "title": "Desktop Notifications",
          "type": "boolean"
        }
      },
      "title": "HudPrefs",
      "type": "object"
    }
  },
  "description": "Prefs file ~/.anqa/config.toml. Authoring format is TOML; this schema describes the same keys for editors and anqa config validate.",
  "properties": {
    "theme": {
      "default": "auto",
      "description": "auto (terminal, then desktop) or a catalog / ~/.anqa/themes name.",
      "title": "Theme",
      "type": "string"
    },
    "follow_os": {
      "default": false,
      "description": "Named pairs follow the host light/dark setting.",
      "title": "Follow Os",
      "type": "boolean"
    },
    "auto_anqad": {
      "default": true,
      "description": "Detach-start anqad when the control socket is free.",
      "title": "Auto Anqad",
      "type": "boolean"
    },
    "live_refresh_workers": {
      "default": 1,
      "description": "Live timeline refresh pool size.",
      "minimum": 1,
      "title": "Live Refresh Workers",
      "type": "integer"
    },
    "hud": {
      "$ref": "#/$defs/HudPrefs"
    },
    "export": {
      "$ref": "#/$defs/ExportPrefs"
    },
    "catalog": {
      "$ref": "#/$defs/CatalogPrefs"
    }
  },
  "title": "anqa-config",
  "type": "object",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://indynull.github.io/anqa/schemas/config.schema.json"
}

rules:
  - id: demo-error-shell
    description: Terminal command ended in error
    category: Demo
    severity: medium
    enabled: true
    detector: demo_error_shell
    params: {}
    summary: "Failed shell: {command}"
    detail: "run_terminal_command reported an error."
    recommendation: "Inspect stderr and fix the command."

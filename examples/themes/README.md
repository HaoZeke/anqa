# User themes

Copy a `*.toml` into `~/.anqa/themes/`. The file stem is the catalog
name (`paper.toml` → `theme = "paper"`). Same file feeds the terminal
app and the desktop HUD.

```bash
mkdir -p ~/.anqa/themes
cp examples/themes/paper.toml ~/.anqa/themes/
```

# Copy to ~/.groket/notes_schema.toml and edit field ids/labels for your workflow.
# Field ids must match ^[A-Za-z_][A-Za-z0-9_-]*$
# Groket does not hardcode review-program form fields — this file owns the layout.

schema_id = "default"

[[fields]]
id = "summary"
label = "Summary"

[[fields]]
id = "detail"
label = "Detail"

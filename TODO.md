# TODO

- Keep `make lint` and `make test` green.
- Split oversized modules when PLR fires (`orchestrator`, large screens) — no blanket `noqa`.
- Prefer top-level imports when editing a file (CLI may still lazy-import the TUI).
